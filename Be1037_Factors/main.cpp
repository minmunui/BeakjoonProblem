#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int testCase = 0;
    cin >> testCase;
    double num;
    int result = 0;

    for ( int i = 0 ; i < testCase ; i ++ ) {
        cin >> num;

    }
}
